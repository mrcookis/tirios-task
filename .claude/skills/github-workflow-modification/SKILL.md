---
name: github-workflow-modification
description: Compiles a github agentic workflow by compiling its content
---

# Instructions

After workflow *.md files is created or modified run: `gh aw compile arg` where `arg` is a worflow name
Where a name is a file name only without extension.
For example, for the file `daily-repo-status.md` the command would be `gh aw compile daily-repo-status`